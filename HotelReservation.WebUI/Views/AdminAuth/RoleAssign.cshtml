@model List<RoleAssignVM>
@{
    ViewData["Title"] = "Set Role(s) for Admin";
    Layout = "~/Views/Shared/_adminLayout.cshtml";
}

<div class="page-wrapper">
    <div class="content">
        <div class="row">
            <div class="col-sm-12">
                <h4 class="page-title">@ViewData["Title"]</h4>
            </div>
        </div>
        <div class="row">
            <div class="col-sm-8">
                <div class="card-box">
                    <div class="card-block">
                        <p class="content-group">
                            <a asp-controller="User" asp-action="kurtulusocL" class="btn btn-outline-primary btn-sm btn-rounded">User List</a> - <a asp-controller="User" asp-action="UserManage" class="btn btn-outline-dark btn-sm btn-rounded">User Manager</a>
                        </p>
                        <div class="table-responsive">
                            <form asp-action="RoleAssign" asp-controller="AdminAuth" method="post">
                                <table class="table table-hover">
                                    <thead>
                                        <tr>
                                            <th>Select to Role(s)</th>
                                            <th>Role Name</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @for (int i = 0; i < Model.Count; i++)
                                        {
                                            <tr>
                                                <td><input type="checkbox" asp-for="@Model[i].HasAssign" /> </td>
                                                <td>
                                                    @Model[i].RoleName
                                                    <input type="hidden" asp-for="@Model[i].RoleId" />
                                                    <input type="hidden" asp-for="@Model[i].RoleName" />
                                                </td>
                                            </tr>
                                        }
                                        <tr>
                                            <td>
                                                <button type="submit" class="btn btn-outline-success btn-sm">Set Role</button>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>