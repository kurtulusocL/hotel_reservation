@model IEnumerable<SocialMedia>
@{
    Layout = null;
}

@foreach (var item in Model)
{
    <a href="@item.Url" class="btn-social-icon " target="_blank">
        <img src="~/img/us/socialMedia/@item.Image" style="max-height:15px;max-width:15px" alt="@item.Image" title="@item.Name">
    </a>
}