@model IEnumerable<Pricing>
@{
    Layout = null;
}

@foreach (var item in Model)
{
    <li>
        <div class="contact-cont">
            <div class="contact-info">
                <span class="contact-name text-ellipsis">@item.Accomodation</span>
                <span class="contact-date">@item.Price</span>
            </div>
        </div>
    </li>
}